{
  "name": "kinda-real-cardgame-modularized",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "rng:gen": "node ../tools/gen_rng_baselines.mjs"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}

