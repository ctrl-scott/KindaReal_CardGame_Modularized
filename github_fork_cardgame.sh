# 0) (once) Make sure you're logged in
gh auth login

# 1) Fork the original repo under your account, and name the fork exactly as desired.
#    This also clones the fork locally into a directory with the same name.
gh repo fork ctrl-scott/KindaReal_CardGame \
  --fork-name KindaReal_CardGame_Modularized \
  --clone

